<style>
  body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f5f5f5;
  }

  .header {
    background-color: #2e8b57;
    color: #fff;
    padding: 20px;
    text-align: center;
  }

  .container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    align-items: center;
    margin: 20px;
  }

  .widget {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 30%;
    max-width: 300px;
    height: 200px;
    padding: 20px;
    background-color: white;
    border-radius: 5px;
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.3);
    margin: 1.5%;
  }


  h2 {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 10px;
  }

  p {
    font-size: 18px;
    text-align: center;
    margin: 0;
  }
</style>

<h1 class="header">Messing with 1M users</h1>

<div class="container">
  <div class="widget">
    <h2>Performance Bite #1</h2>
    <p>Large collection of records</p>
    <br>
    <a href="<%= large_records_collection_path %>">Use #each, #find_each and #all</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #2</h2>
    <p>Select values for one column</p>
    <br>
    <a href="<%= one_column_path(way: :not_recommended) %>">Use #map</a>
    <a href="<%= one_column_path(way: :recommended) %>">Use #pluck</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #3</h2>
    <p>Counting Records</p>
    <br>
    <a href="<%= counting_records_path(way: "length") %>">Use #length</a>
    <a href="<%= counting_records_path(way: "count") %>">Use #count</a>
    <a href="<%= counting_records_path(way: "size") %>">Use #size</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #4</h2>
    <p>Bulk Insert and Update</p>
    <br>
    <a href="<%= bulk_insert_path(way: "each") %>">Use #each (loop)</a>
    <a href="<%= bulk_insert_path(way: "insert_all") %>">Use #insert_all</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #5</h2>
    <p>Ruby Operations V/S SQL operations</p>
    <br>
    <a href="<%= ruby_vs_sql_ops_path(way: "ruby") %>">Use Ruby</a>
    <a href="<%= ruby_vs_sql_ops_path(way: "sql") %>">Use SQL</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #6</h2>
    <p>Checking the Record presence</p>
    <br>
    <a href="<%= records_presence_path(way: "exists") %>">Use #exists?</a>
    <a href="<%= records_presence_path(way: "present") %>">Use #present?</a>
    <a href="<%= records_presence_path(way: "collection.load") %>">Use @collection.load.any?</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #7</h2>
    <p>Bang Methods with Transactions</p>
    <br>
    <a href="<%= records_transactions_path %>">Use #create or #create!</a>
  </div>
  <div class="widget">
    <h2>Performance Bite #8</h2>
    <p>Difference b/w #delete and #destroy</p>
    <br>
    <a href="<%= records_deletion_path %>">#delete or #destroy</a>
  </div>
</div>
